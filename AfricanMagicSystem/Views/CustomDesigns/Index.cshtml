@model IEnumerable<AfricanMagicSystem.Models.CustomDesign>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.InternalImage)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.InternalImage)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DesignNumber }) |
            @Html.ActionLink("Details", "Details", new { id=item.DesignNumber }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DesignNumber })
        </td>
    </tr>
}

</table>
